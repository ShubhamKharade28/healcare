const { default: Background } = require("../components/Background")

const Aboutus = () => {
    return (
        <main className="w-screen min-h-screen relative">
            <Background />
        </main>
    )
}